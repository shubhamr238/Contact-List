<!DOCTYPE html>
<html lang="en">
<head>
    
    <title>
        <%=title %>
    </title>
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    
    <h1 id="page-heading">
        <i class="fas fa-address-book"></i>
        My Contacts List
    </h1>
    <div id="contact-list-container">

        <!-- <table>
            <tbody>
                <% for(let i of contact_list){ %>
                    <tr>
                        <td><%= i.name %></td>
                        <td class="right"><%=i.phone %></td>
                        <td class="right">
                            <form action="/contacts/<%= i.id %>?_method=DELETE" method="POST">
                                <button><i class="fas fa-window-close"></i></button>
                            </form>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table> -->
        <ul>
            <% for(let i of contact_list){ %>
                <li class="contact">
                    <div class="name">
                        <p><%= i.name %></p>
                    </div>
                    <div class="number-and-del">
                        <p><%=i.phone %></p>
                        <form action="/contacts/<%= i.id %>?_method=DELETE" method="POST">
                            <button><i class="fas fa-window-close"></i></button>
                        </form> 
                    </div>
                    <!-- <div class="delete-button">
                        
                    </div> -->
                </li>
            <% } %>
        </ul>
    </div>
    <form id="new-contact-form" action="/contacts" method="POST">
        <input type="text" name="name" maxlength="20" placeholder="Enter Name" required>
        <input type="number" name="phone" max="999999999999" placeholder="Enter Phone" required>
        <button type="submit">Add</button>
    </form>
    <script src="/js/home.js"></script>

</body>
</html>